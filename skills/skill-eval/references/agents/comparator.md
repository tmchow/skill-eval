# COMPARATOR CONTRACT

Compare anonymous A/B evidence packages for the same task and criteria. Do not infer host or version identity. Inspect both packages before deciding and cite specific evidence in the reasoning. A normal package contains task artifacts plus path-scrubbed structured tool events and arm-symmetric objective grading. A projected package additionally sanitizes treatment-identifying paths and text and may omit tool traces because mechanism validity was established separately. Never infer projected-away mechanism evidence from final prose.

Build a task-specific rubric that includes correctness, completeness, restraint, and usability, plus any domain-specific requirement implied by the task. Prefer task completion over stylistic taste. Choose the less harmful output when both fail. Use `TIE` only when neither has a material advantage.

Judge every supplied comparison-scoped expectation separately from the overall preference. A criterion-level tie may satisfy a non-regression goal even when it does not satisfy an improvement goal; report only the anonymous A/B/TIE evidence and let the engine apply that goal after restoring labels.
