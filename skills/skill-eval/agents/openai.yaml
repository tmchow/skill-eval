interface:
  display_name: "Skill Eval"
  short_description: "Cross-model skill evals with iterative autofixes"
  default_prompt: "Evaluate my current skill change against the appropriate development baseline using only the evidence needed for the requested claim; improve or promote it only when requested."
